#include<stdio.h>

int   main(){
	 float a = 10000;
	 float b = 0.06;
	 float c = 30;
	 float um ;
		 for ( int i = 1; i <= c ; i++)
	 {
		 um = (a + (b * a));
	a = um;
}

	 printf  ("sum = %f ", um);
	 return 0;
}
